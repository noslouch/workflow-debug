{
  "name": "@noslouch/monorepo",
  "description": "",
  "author": "",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": "^14.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:noslouch/workflow-debug.git"
  },
  "scripts": {
    "prepare": "lerna run --stream --sort prepare",
    "rebuild": "lerna exec --stream --sort -- npm rebuild"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/**"
  ]
}
